{
  "name": "nodejs-server",
  "version": "1.0.0",
  "description": "const express = require('express');\r const axios = require('axios');",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.6",
    "express": "^4.18.2"
  }
}
